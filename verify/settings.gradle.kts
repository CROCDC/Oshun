// Standalone JVM build used only to unit-test the pure-Kotlin NMEA core on a
// machine without the Android SDK. It reuses the exact same source files as the
// Android app module (see build.gradle.kts sourceSets).
rootProject.name = "oshun-nmea-verify"
